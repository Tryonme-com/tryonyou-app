# ── App ───────────────────────────────────────────────────────────────────────
APP_ENV=development          # development | staging | production

# ── Stripe ────────────────────────────────────────────────────────────────────
STRIPE_API_KEY=sk_test_xxx
STRIPE_ENDPOINT_SECRET=whsec_test_xxx

# ── Database ──────────────────────────────────────────────────────────────────
TRYONYOU_DB_PATH=/tmp/tryonyou_leads.sqlite

# ── Production queue (replace in-memory stores when ready) ───────────────────
# REDIS_URL=redis://localhost:6379/0
# QUEUE_NAME=stripe_events

# ── Jules worker (uncomment when wiring async processing) ────────────────────
# JULES_WORKER_URL=https://your-jules-worker/run
# JULES_API_KEY=jules_xxx
