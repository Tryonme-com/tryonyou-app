#!/usr/bin/env bash
# Wrapper soberano: Supercommit_Max -> autonomia_total_tryonyou.py
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "${ROOT}/autonomia_total_tryonyou.py" "$@"
