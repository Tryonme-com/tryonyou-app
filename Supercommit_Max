#!/usr/bin/env bash
# Alias soberano de Supercommit_Max. Delega en el wrapper no destructivo.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$ROOT/supercommit_max.sh" "$@"
