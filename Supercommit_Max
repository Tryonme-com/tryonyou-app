#!/usr/bin/env bash
# Entrypoint soberano: mantiene compatibilidad con operaciones que invocan
# Supercommit_Max y delega en el flujo seguro versionado.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$ROOT/supercommit_max.sh" "$@"
