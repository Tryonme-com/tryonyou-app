#!/usr/bin/env bash
# Wrapper histórico: conserva el nombre solicitado y delega en el flujo seguro.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$ROOT/supercommit_max.sh" "$@"
